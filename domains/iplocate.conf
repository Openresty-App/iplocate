#short url
server {
    listen       10905;
    server_name  localhost;

    lua_code_cache on;

    location = /health {
        echo '{"status": 0, "message":"ok"}';
    }

    location ~ /.* {
        content_by_lua_file lua/web/iplocate.lua;
    }
}
